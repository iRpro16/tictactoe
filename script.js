const gameboard = (() => {
   const boardArr = ['', '', '', '', '', '', '', '', '']; 
   return {
    boardArr
   };
})();

const displayBoard = (() => {

})();

const playerFactory = (name, marker) => {
    return {name, marker}
};